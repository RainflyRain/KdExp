// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN
buildscript {
	repositories {
		google()
		jcenter()
	}



	dependencies {
		classpath 'com.android.tools.build:gradle:3.0.0'
	}
}


allprojects {
   repositories {
	   //调用方需要引用被调用方的仓库代码
	   maven { url 'http://raw.github.com/saki4510t/libcommon/master/repository/' }
	   jcenter()
	   google()
      flatDir {
        dirs 'libs'
      }
   }
}

apply plugin: 'com.android.application'

dependencies {
	compile fileTree(include: ['*.jar'], dir: 'libs')
	//compile(name: 'myunitylib-debug', ext: 'aar')


	//for uvc start
//	compile project(':libuvccamera')
//	implementation 'com.android.support:appcompat-v7:26.0.0-beta1'
//	implementation 'com.android.support.constraint:constraint-layout:1.0.2'
	//for uvc end

//	compile project(':libkudancv')
}


android {
	compileSdkVersion 27
	buildToolsVersion '27.0.1'

	defaultConfig {
		minSdkVersion 21
		targetSdkVersion 27
		applicationId 'com.pacificfuture.artourism'
	}

	lintOptions {
		abortOnError false
	}

	aaptOptions {
		noCompress '.unity3d', '.ress', '.resource', '.obb'
	}

	signingConfigs { release {
		storeFile file('/Users/fly/Documents/work/文档/签名证书/rain.jks')
		storePassword 'kb_2015'
		keyAlias 'fly'
		keyPassword 'kb_2015'
	} }

	buildTypes {
  		debug {
 			minifyEnabled false
 			useProguard false
 			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
  			jniDebuggable true
  		}
  		release {
 			minifyEnabled false
 			useProguard false
  			proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
  			signingConfig signingConfigs.release
  		}
	}

}
